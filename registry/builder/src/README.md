# registry-builder/src

Source code for the registry builder lives here.

This should stay separate from the main `plshelp` CLI runtime so scheduled registry generation does not complicate the CLI binary.
