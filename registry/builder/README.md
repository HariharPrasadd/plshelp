# registry-builder

This folder will contain the periodic registry build pipeline.

Planned responsibilities:
- read registry source definitions
- crawl and clean documentation
- generate `registry/index.json`
- generate `registry/docs/<slug>/docs.md` and `docs.txt`
