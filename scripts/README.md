# scripts

Installer and release helper scripts live here.

Planned contents:
- `install.sh` for macOS/Linux install flow
- `install.ps1` for Windows PowerShell install flow
- optional local release helper scripts later
