# .github/workflows

GitHub Actions workflows live here.

Planned workflows:
- `release.yml` for building and publishing binaries
- `site.yml` for deploying the website
- `registry.yml` for periodic registry generation and publishing
